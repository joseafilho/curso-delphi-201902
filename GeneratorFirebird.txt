# Criando o Sequence.
CREATE SEQUENCE [NOME_SEQUENCE_SEQ]

# Criando a trigger
CREATE TRIGGER [NOME_TRIGGER]_TR FOR [TABLE_NAME] BEFORE INSERT
AS
BEGIN
	IF (NEW.[NOME DA COLUNA] IS NULL) THEN
	  NEW.[NOME DA COLUNA] = GEN_ID([NOME DO SEQUENCE]_SEQ, 1);
END